---
---
<Breadcrumbs >
    {items.map((item)=>(
        <BreadcrumbsItem
            href="/"
            label={item}
            curretPage={true}
        />
    ))}
</Breadcrumbs>

<style lang="scss" is:global>
    body .breadcrumbs__item {
        li::after {
            content: '/';
        }

      a {
        font-size: 30px;
        
      }
    }
</style>
